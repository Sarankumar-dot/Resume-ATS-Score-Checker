{
  "name": "resume-ats-checker",
  "version": "0.1.0",
  "private": true,
  "description": "Resume ATS Checker — Know if your resume passes the ATS and how to fix it.",
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
